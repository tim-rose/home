#
# Makefile --Build rules for "a2ps", customisations for the a2ps utility..
#
prefix = $(HOME)
sysconfdir = $(HOME)/.a2ps

SRC = a2psrc bw.pro

include makeshift.mk

install:	$(SRC:%=$(sysconfdir)/%)
